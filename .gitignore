# 1. Create a .gitignore file with standard Terraform ignores.
#    (This is a PowerShell command to create the file)
Set-Content .gitignore @"
# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json

# Ignore override files as they are usually temporary
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include the lock file so everyone uses the same provider versions
!.terraform.lock.hcl
"@
